# Firebase 設定

## 這一版改了什麼（重要）

- 登入改成**姓名 + 密碼**；新名字第一次進來時自己設定密碼（至少 8 個字元），**已經沒有共用的預設密碼**
- 聊天室改成**兩人一組**：從線上名單邀請對方，對方同意才開始
- **對話紀錄只有那兩個人看得到**，每一組各自獨立
- **題庫也是每組獨立**，沒有自己的題庫時就用內建預設題目
- 匿名登入從「可選」變成**必要**（帳號機制靠它綁定），請務必啟用

⚠️ **安全規則一定要重新發布**，否則登入會直接失敗。

---

## 目前狀態：邀請碼閘門「關閉」

現在不需要做任何帳號設定就能使用。唯一必要的動作是**部署一次安全規則**。

---

## 現在要做的（只有這一件）

把 `firestore.rules` 的內容貼到
Firebase Console → **Firestore Database** → **規則** → 發布。

或用 CLI：

```bash
firebase deploy --only firestore:rules
```

規則裡的閘門預設是關的（找不到 `config/access` 這份文件就視為關閉），
所以發布後 App 照常運作，不會被擋。

> 沒發布規則的話，資料庫仍是誰都能讀寫的狀態。

---

## 帳號與密碼

| 集合 | 文件 ID | 內容 | 誰讀得到 |
| --- | --- | --- | --- |
| `users` | 姓名 | `name`、`hasRecoveryEmail`、`salt`、`hashVersion`、`mustChangePassword` | 所有人**單筆查詢**（不能列出整個集合），裡面沒有任何私人資料 |
| `accountPrivate` | 姓名 | `email`（救援 Email 本人） | 只有本人 |
| `secrets` | 姓名 | `passwordHash` | **沒有人**，只有安全規則讀得到 |
| `sessions` | 匿名 UID | `name`、`passwordHash` | 只有自己 |
| `emails` | Email（小寫） | `key`（對應哪個姓名） | 只有剛用該 Email 完成 Firebase 驗證信流程的瀏覽器；已登入的人可以查「這個地址是不是已被別人用」，但看不到是誰 |

沒有後端可以驗密碼，所以驗證放在規則裡：登入時把雜湊寫進 `sessions/{uid}`，
規則會拿它跟 `secrets/{姓名}` 比對，**不一致就整筆寫入被拒絕**。
前端擋不住的事情（改 JS、開 devtools）在這裡擋得住。

- **姓名不分大小寫**：`Amy`、`amy`、`AMY` 是同一個帳號。文件 ID 一律用小寫，顯示名稱則保留註冊時的寫法
- 姓名同時是文件 ID，所以**不能包含斜線**（規則沒辦法做 URL 編碼，只好從源頭限制）
- **忘記密碼**：如果這個帳號有設定救援 Email，登入畫面的「忘記密碼？」就能自己重設，見下方〈忘記密碼〉一節。沒設定過的話，還是只能到 Console 手動刪掉 `secrets/{姓名}` 與 `users/{姓名}`，下次登入等於新帳號（要重新設定密碼）
- **手動清掉救援 Email 記得連 Auth 一起清**：光刪 Firestore 的 `accountPrivate/{姓名}` 沒用，下次登入會被 `syncVerifiedEmail` 自動補回來——因為 Firebase Auth 那邊還連著一組已驗證的憑證。要連 Authentication → Users 裡對應的匿名使用者（uid 可以從 `sessions` 集合裡該姓名那筆文件的 ID 找到）一起刪掉
- 清掉瀏覽器資料後匿名 UID 會換一組，`sessions` 就對不上，App 會請你重新登入——密碼還是原來那組

### 一個瀏覽器只能登入一個帳號

匿名 UID 是整個瀏覽器共用的，`sessions` 又是以 UID 為鍵，
所以在同一個瀏覽器的第二個視窗登入另一個帳號，會把第一個視窗頂掉。
要兩個人同時測試，請用**兩個不同的瀏覽器**或無痕視窗。

同一個帳號開兩個視窗則沒問題，而且**線上人數不會因此變成 2**（改成算不重複的姓名）。

---

## 忘記密碼

登入畫面（既有帳號的密碼那一步）多了「忘記密碼？」，點進去要**輸入自己的救援 Email**（Email 現在是私人資料，不能再用姓名去查）；登入後每次也會被（可跳過的）彈窗提醒設定救援 Email——沒有 Email，忘記密碼就真的沒辦法自救。

寄信這件事完全靠 **Firebase Authentication 內建的 Email 連結（passwordless email link）機制**，沒有裝任何寄信套件、沒有自己的後端、也不需要另外申請 Email 服務。之所以不是更直覺的「密碼重設信」（`sendPasswordResetEmail`），是因為那種信的連結預設會先跳到 Firebase 自己的托管頁面完成重設，要跳過那個頁面、直接進這個網站，需要在 Console 開「自訂動作網址」（Customize action URL）——而這個專案的範本編輯功能被 Firebase 鎖住過，鎖不鎖是帳號層級的限制，不保證解得開。Email 連結登入的信件天生就沒有 Firebase 自己的頁面可以顯示，一定會直接跳回這個網站，才能繞過這個限制。代價是**信件內容本身固定用 Firebase 的預設格式，Console 改不了**。

運作方式，依情境分三種：

**設定 Email 前**：不管是第一次設定還是改成另一個，都會先查一次 `emails/{email}`：地址沒人用或是自己的就繼續；是別人的，規則會拒絕這次查詢，App 就當作「已被使用」擋下來、不寄任何信（`accounts.ts` 的 `isEmailTaken`）。真正保證不重複的還是 `emails/{email}` 的建立規則。

**第一次設定救援 Email**：呼叫 `sendSignInLinkToEmail` 寄一封確認信到這個新 Email；點裡面的連結會回到這個網站，做一次 `signInWithEmailLink`（純粹只是「證明這個地址是我的」，跟這個瀏覽器原本的登入狀態無關，任何裝置點開都行）。原本想用 `EmailAuthProvider.credentialWithLink` + `linkWithCredential` 把憑證直接掛到目前這個匿名帳號上、當場寫 Firestore——實測（對著 Auth 模擬器）發現這個組合並不會真的「掛上去」，而是默默登入成另一個獨立帳號，導致後續步驟必定失敗（`auth/invalid-action-code`），所以放棄這條路，改成跟下面「改 Email」共用同一套機制：先不碰 Firestore，靠 `syncVerifiedEmail` 事後補上。

**改成另一個 Email**（原本已經有設定過）：先寄一封確認信到**舊的** Email，點連結表示「我同意換」；同意之後才會接著寄第二封信到新 Email，點了才算真的換成功。要求先經過舊信箱同意，是為了防止有人借用已登入的瀏覽器把救援 Email 偷換成自己的——光靠新信箱自己按確認是擋不住這種情況的。

**`syncVerifiedEmail`**：不管是上面哪一種，點完信裡的連結當下都不會直接改 Firestore——當下那個分頁不一定跟原本登入的裝置是同一個 session，直接寫很可能被規則擋下來。所以改成每次正常登入時，順便檢查一次「Firebase 那邊現在的 Email 是不是已驗證、且跟 Firestore 記錄的不一樣」，不一樣就自動同步過去——換句話說，**點完信裡最後一個連結之後，要等下一次重新登入才會真的生效**，不是點完馬上生效。

> 這兩個「點連結後自動觸發下一步」的畫面（`EmailChangeReauthView`、`NewEmailConfirmationView`）在 `useEffect` 裡呼叫的都是**一次性**的動作碼（用過就失效）。React 的 `<StrictMode>` 在開發模式會刻意把 effect 重複呼叫一次，藉此抓出這種「呼叫兩次會壞掉」的副作用——第一次呼叫其實會成功，但緊接著的第二次呼叫會因為動作碼已經用過而失敗，如果沒有特別處理，畫面顯示的會是那個誤導人的第二次失敗，讓人誤以為整個功能壞了。兩個元件都用一個 `useRef` 擋掉第二次呼叫，只保留真正成功的第一次結果——如果之後要再寫類似「點連結自動執行一次性動作」的畫面，記得比照辦理。

「忘記密碼？」本身則是：輸入救援 Email，寄一封 Email 連結登入信過去（畫面一律回答「已寄出」，不會透露這個地址有沒有對應到帳號）；點連結進到這個網站的「設定新密碼」畫面，同時完成三件事：Firebase 那邊的密碼、這個 App 自己用的密碼雜湊（`secrets/{姓名}`）、還有登入這個瀏覽器——一步到位，不用再手動登入一次。

### 需要在 Firebase Console 做的一次性設定

**Authentication → Sign-in method → 電子郵件/密碼 → 打開裡面的「Email link (passwordless sign-in)」子開關**（不是新增供應商，是點進既有的「電子郵件/密碼」那一列才看得到）。這是唯一要手動做的事——沒開的話，設定 Email 跟忘記密碼都會直接 400 失敗（`auth/operation-not-allowed`）。

也要確認 Authentication → Settings → Authorized domains 裡有你測試/正式用的網域（`localhost`、正式網域都要各自加），不然寄信會直接失敗（`auth/unauthorized-continue-uri`）。

### 安全模型

`secrets/{姓名}` 原本只有「登入中的自己」能改；忘記密碼開了第二條路：**證明自己剛完成 Firebase 驗證信流程、且該 Email 在 `emails/{email}` 對應到這個姓名**，見 `firestore.rules` 的 `canResetByEmail`。沒有 Email 索引、Email 沒驗證、或 Email 對到別的姓名，一律被拒絕——已經寫進 `tests/firestore.rules.test.mjs` 的「Forgot password」那組測試，`npm run test:rules` 會一起跑到。

設定 / 更改救援 Email 的兩種確認連結（見上方〈忘記密碼〉），任何裝置點開都行，不需要跟原本申請的瀏覽器一樣——它們只負責向 Firebase 證明「這個地址是我的」，從不直接碰 Firestore，真正落地永遠是 `syncVerifiedEmail` 在下一次正常登入時做的，那時候當然是在使用者自己已登入的裝置上，`hasSession()` 自然成立。

---

## 資安加固（弱點測試報告之後）

這一節記錄 `security/fixes` 這批修補做了什麼、要照什麼順序上線、還剩什麼沒處理。詳細的逐項結果在 `SECURITY-REPORT.md`。

### 上線順序（很重要）

1. **先部署 Firestore 規則**（Firebase Console → Firestore → 規則，貼上 `firestore.rules` 全文 → 發布；或 `firebase deploy --only firestore:rules`）。
   新規則對「還沒更新的舊版網站」是相容的：舊版查 Email 重複的那段會失敗、但它本來就是「失敗就放行」；其餘功能照常。
2. **再合併 `security/fixes` 到 `main`**，GitHub Actions 會自動部署新版網站。
3. 之後每個舊帳號**下次登入時會自己升級**：密碼改存加鹽 + PBKDF2、公開的 Email 搬到私人文件。不需要手動搬資料。

反過來（先發網站、後發規則）會有一段時間新版網站的「Email 重複檢查」一律被舊規則拒絕、把任何地址都判成「已被使用」，所以請照上面的順序。

### 上線後請檢查

- Console → Firestore → `users`：找 **`mustChangePassword` 為 `true`** 的帳號。這些是還停在舊預設密碼 `0101` 的帳號，**任何人都能用 `0101` 登入**。沒在用的直接刪掉（連同 `secrets/{姓名}`），還在用的請本人盡快登入改密碼。這是舊資料的問題，程式碼改完後才不會再產生新的，但舊的只能人工清。
- `users` 文件裡如果還看得到 `email` 欄位，代表那個帳號還沒再登入過；等本人下次登入就會自動移走。想更快處理，可以請對方登入一次。
- 已經公開過的 Email 沒辦法收回。如果覺得需要，可以請大家換一組救援 Email。

### App Check（擋掉站外腳本直接打 Firebase）

程式已經內建，但**預設不啟用**（沒有金鑰就完全不動作）。要開的話：

1. Firebase Console → App Check → Apps → 這個網頁應用程式 → 選 **reCAPTCHA v3**（或 Enterprise），依指示到 reCAPTCHA 後台建立金鑰，網域填 `wu-haowei.github.io`。
2. 把**網站金鑰（site key）**填到 GitHub → Settings → Secrets and variables → Actions → Variables，名稱 `VITE_APPCHECK_SITE_KEY`；密鑰（secret key）貼回 Firebase Console。
3. 重新部署。先**不要按「強制執行」**，到 App Check 的指標頁看「已驗證的請求」比例，正常使用一兩天、確認幾乎都被驗證了，再對 **Cloud Firestore** 按強制執行。
4. 本機對著正式專案開發時，把 App Check → 管理偵錯權杖 產生的 token 放進 `VITE_APPCHECK_DEBUG_TOKEN`。模擬器不受影響。

Authentication（匿名登入）要不要強制 App Check，需要升級 Identity Platform，不建議現在做。改用下面這個更便宜的做法：

- Google Cloud Console → 帳單 → **預算與快訊**，設一個低額預算與 50% / 90% / 100% 通知。
- Firebase Console → Authentication → 使用情況，偶爾看一下匿名帳號數量有沒有異常暴增。

### 安全標頭

GitHub Pages 不能自訂回應標頭，所以：

- 已用 `<meta>` 加上 **Content Security Policy**（只限打包出來的網站本身、Firebase／Google API、reCAPTCHA），與 `Referrer-Policy: strict-origin-when-cross-origin`；只在正式 build 生效，開發模式不受影響。CSP 寫在 `vite.config.ts` 的 `contentSecurityPolicy`。**之後如果新增外部服務（字型、圖片來源、其他 API）而網站壞掉，是要把那個網域加進這裡。**
- `frame-ancestors`、`X-Frame-Options`、`X-Content-Type-Options: nosniff` 這幾個 meta 標籤無效，做不到。點擊劫持的部分用 `public/frame-guard.js` 補：被別人的網頁用 iframe 包起來時，會把頁面藏起來並嘗試跳出。這是盡力而為的做法（框住的一方可以用 `sandbox` 讓腳本不執行），真正的做法是搬到能設標頭的主機（例如 Firebase Hosting，`firebase.json` 就能設）。

## 配對聊天室

房間 ID 由兩個姓名排序後組成，例如 `pair__amy__bob`，兩邊各自算得出同一組，
不需要交換代號。房間文件裡的 `participants` 是規則判斷「誰能看」的依據。

流程：線上名單 → 邀請 → 對方同意 → 房間建立 → 開始聊天。

`presence` 集合每人一份文件，心跳 60 秒一次，而且**只有在對話列表頁才會監聽**——
進到房間就取消訂閱，否則每個心跳都會變成每台裝置的一次讀取。

---

## LOGO 的手勢

| 次數 | 行為 |
| --- | --- |
| 一下 | 回到對話 |
| **三下** | 顯示／收起後台的「預設題庫」切換，並跳到後台 |

兩者都是**當下就反應**：第一下立刻切到對話，計數器在底下繼續跑；
第三下一落地就執行，不用等計時器。安靜約 0.3 秒之後計數自動歸零。

---

## 題庫：每組獨立，外加一份共用的預設

| 題庫 | 存在哪裡 | 誰看得到 |
| --- | --- | --- |
| 這一組的 | `rooms/{房間}/faqs` | 只有那兩個人 |
| **預設題庫** | 根層級的 `faqs` | 所有人共用 |

一組還沒有自己的題庫時，就借用預設題庫來玩（**不會寫進這組的資料**）。
只要新增或匯入一題，就變成這組自己的題庫，之後跟預設題庫再無關係。

### 預設題目不在程式裡

以前那 12 題是寫死在 `src/data/initialData.ts` 的常數，改一題就要重新部署網站。
現在整份存在 Firestore，**後台改完立刻生效**。

後台管理標題下方有個切換：**「這組的題庫 / 預設題庫」**。
切到預設題庫之後，新增、編輯、刪除、匯入、匯出全部照用。

**這個切換平常不會顯示**——預設題庫是所有人共用的，在那裡改錯是唯一會波及
其他對話的失誤。要**連點三下 LOGO** 才會出現——解鎖時會順便跳到後台，因為那是唯一會做這個手勢的
理由。再連點三下收起來，同時切回這組的題庫。解鎖狀態不會記住，重新整理就收回去了。

切到預設題庫時，只對單一對話有意義的工具會**自動隱藏**——完整備份、從備份還原、
搬移舊版資料、刪除答過的。預設題庫是共用的，沒有「這一組」可言。

> **第一次部署要做一次匯入。** 程式裡已經沒有題目了，所以預設題庫是空的。
> 專案根目錄附了 `default-faqs.json`（原本那 12 題），
> 到後台切到「預設題庫」→ 匯入題目 → 選檔案，一次就好。

### 讀取成本

預設題庫**只在真的需要時才讀**：這一組沒有自己的題庫、或按下「匯入預設題目」時，
各做一次性讀取。已經有自己題庫的組完全不會碰到它。
只有後台切到「預設題庫」時才會掛即時監聽，好讓你邊改邊看到。

### 把這一組還原成預設題庫

後台管理 → 工具列的**還原箭頭圖示**。

⚠️ 這是**取代不是合併**：會先刪掉這一組現有的題目，再整份寫入預設題庫。
房間文件上的 `playedFaqIds`（「答過了」的紀錄）也會一起清空——
那些 id 指向的題目已經不存在，而且那份清單沒有其他縮小的機會。

**對話紀錄與出題歷史不受影響**，只動題庫。

> 預設題庫是空的時候會直接中止，不會先把現有題庫刪掉再發現沒東西可寫。

### 從舊版搬資料

後台工具列的**時鐘圖示**會把舊的共用房間 `MAIN-ROOM` 的下列東西複製到目前這一組：

| 內容 | 存在哪裡 |
| --- | --- |
| 對話紀錄 | `messages` 子集合 |
| 出題紀錄 | `rounds` 子集合 |
| 題庫 | `faqs` 子集合，以及舊的共用 `faqs` |
| **已玩過的題目** | 房間文件上的 `playedFaqIds` 欄位 |

最後一項容易漏掉——它不是子集合，而是直接掛在房間文件上（隨機抽題的
「排除已玩過」就是讀它），所以只複製子集合的話，所有題目都會變回沒玩過。

- 是**複製不是搬移**，舊資料原封不動留著
- 重複執行不會產生重複資料（同 ID 會跳過）
- 兩個人的其中一個做一次就好

---

## 多語系（繁體中文／English／日本語）

介面文字與題目內容都支援三種語言，預設繁體中文。**匯入題目不變**：匯入時照原樣存，不翻譯、不改格式；之後再到後台逐題補翻譯。

### 語言怎麼決定

- 登入頁、各種連結頁、選單裡都有語言切換鈕（`LanguageSwitcher`）。
- 選擇存在瀏覽器（`localStorage` 的 `milktea_qa_lang_v1`），登入後也會存進 `userPrefs/{name}.language`，換裝置登入會跟著走。
- 兩個人各自用自己的語言：同一題，一個人看中文、另一個人看日文，互不影響。

### 介面文字

- 字典在 `src/i18n/messages/<區塊>.ts`，每個檔案有 `zh`、`en`、`ja` 三份，`en`／`ja` 的型別是 `Record<keyof typeof zh, string>`，**少一個 key 就編譯不過**。
- 元件裡用 `const t = useT()`；非元件的程式碼用 `t()`（讀當下語言）。參數用 `{name}` 佔位。
- 想要句中夾粗體／紅字，把標記寫進字串（`<b>…</b>`、`<d>…</d>`）再用 `<Rich text={…} />` 顯示，不要把一句話拆成幾段翻。
- 新增語言：在 `src/i18n/index.ts` 的 `Lang`／`LANGS`／`INTL_LOCALE` 加一筆，再把每個 messages 檔補上該語言。

### 題目內容

- 後台編輯題目時，上方有「原文／繁體中文／English／日本語」分頁，可填各語言的題目、說明、選項。
- 資料存在題目的 `translations` 欄位；沒有對應翻譯就顯示原文。
- **選項翻譯必須和原文一對一**（數量相同、每個都填）。答案存的是選項的「位置」，數量對不上會讓兩個人在同一個編號底下看到不同題目，所以對不上時整組選項改用原文。後台儲存時會擋下填一半的選項。
- 開始一輪考驗時，題目與選項的翻譯會一併存進那一輪（`RoomQuestion.translations`），之後題庫再改也不影響進行中的那一題。
- 「答過了」的比對仍然用原文，加翻譯不會讓題目變成沒答過。

### 舊資料與例外

- 系統訊息（誰發起、猜對了…）新資料會帶 `i18n: { key, params }`，各人用自己的語言顯示；舊的紀錄沒有這個欄位，仍顯示當時寫下的中文。
- 已送出的回覆內容是當事人寫下的文字，不會被翻譯。
- 「未分類」「自訂」這兩個分類名稱在資料裡固定是中文，畫面上才翻譯（`displayCategory`）。
- 判斷舊訊息用的中文字串（`系統`、`猜對了`、`說明:` 等）是資料格式的一部分，**不要**改成翻譯過的字。
- Firestore 規則不需要為多語系做任何調整。

## 之後想開啟邀請碼保護

開關是 Firestore 裡的**一個布林值**，前端和安全規則讀的是同一份文件，
所以改完立刻生效，**不用重新部署網站**。

> 帳號是用姓名認領的，閘門關著時任何人都能搶先註冊一個名字。
> 只有你們兩個人在用的話，建議把閘門打開。

### 步驟

**1. 開啟匿名登入**

Authentication → Sign-in method → **匿名 (Anonymous)** → 啟用。
（這一版是**必要**的，不論閘門開或關。）

**2. 建立邀請碼**

Firestore → 新增文件：

| 集合 ID | 文件 ID | 欄位 | 型別 | 值 |
| --- | --- | --- | --- | --- |
| `config` | `invite` | `code` | string | 自訂通行碼 |

**3. 打開開關**

Firestore → 同一個 `config` 集合 → 新增文件：

| 文件 ID | 欄位 | 型別 | 值 |
| --- | --- | --- | --- |
| `access` | `requireInvite` | boolean | `true` |

下一次有人重新整理頁面，就會看到邀請碼畫面。

**4. 兩個人各自輸入一次**

輸入正確的碼之後，該裝置的匿名 UID 會自動寫進 `members` 集合，
之後不用再輸入。

### 要關掉

把 `config/access` 的 `requireInvite` 改成 `false`（或直接刪掉這份文件）。

---

## 白名單管理

白名單就是 `members` 集合，**不需要手動建立**——有人輸入正確邀請碼時會自動產生。

- **踢掉某台裝置**：刪掉對應文件，它下次重新整理就要重新輸入邀請碼
- **換邀請碼**：改 `config/invite` 的 `code`，已在名單內的裝置不受影響
- **清除瀏覽器資料後**：匿名 UID 會換一組，等於新裝置，要再輸入一次

`code` 只有安全規則讀得到，`config/invite` 對用戶端完全禁讀，不會出現在前端程式碼裡。

---

## 從 Google 雲端匯入題目

後台「匯入題目」視窗多了一個「從雲端讀取」按鈕：讀取後台設定好的 Google Drive 資料夾，自動抓裡面**所有** `.json` 檔案並合併成一份清單，不用先下載到本機、也不用每次手動貼連結。讀到的內容會填進下面的文字框，跟平常貼 JSON 一樣，按「匯入」才會真的寫入目前這組的題庫——所以就算某個檔案格式有問題，也是在這一步發現，還沒寫進資料庫。

流程不經過任何後端，是瀏覽器直接呼叫 Google Drive API，靠的是一把限定 Drive API 的 **API 金鑰**，不需要登入 Google 帳號。

### 檔案清單的排序與分組

資料夾裡的檔案會依檔名排序並分組顯示（每組有標題和整組勾選框）。檔名格式：

```
AI名稱_[類型_]questions_yyyymmdd_流水號[_題數].json
```

- 排序：AI 名稱 → 類型 → 日期（新到舊）→ 流水號（新到舊，當數字比，`10` 在 `02` 前面）。AI 名稱和類型不分大小寫，`Claude` 與 `claude` 算同一組
- **類型可以沒有**：沒有類型的檔案歸在「AI · 一般」，排在同一個 AI 的最前面
- **題數（最後那個數字）不固定**，隨便是多少都行，只是不參與排序；結尾多一個底線（`_50_.json`）也沒關係
- 檔名裡一定要有 `questions` 這個字，排序是靠它切開「AI／類型」跟「日期／流水號」的。不符合格式的檔案不會被丟掉，會集中在清單最底下的「其他」組，照檔名排序
- 資料夾檔案超過一頁（Drive 一次最多回 1000 筆）會自動翻頁讀完，排序看到的一定是完整清單

### 設定步驟

**1. 資料夾要設成公開**

Google Drive 資料夾的共用設定選「知道連結的人皆可查看」，否則金鑰讀不到內容。

**2. 建立 API 金鑰**

[Google Cloud Console](https://console.cloud.google.com/) → 選擇這個 Firebase 專案（跟 `VITE_FIREBASE_PROJECT_ID` 同一個即可，不用另外開專案）：

1. **API 和服務 → 程式庫**，搜尋 **Google Drive API**，啟用它
2. **API 和服務 → 憑證 → 建立憑證 → API 金鑰**
3. 建議限制金鑰用途：**API 限制**選只允許 Google Drive API；**應用程式限制**選 HTTP 轉介網址，填上網站的網域（本機開發要加 `http://localhost:3000/*`），避免金鑰被盜用在別的地方

**3. 寫進環境變數**

跟其他 `VITE_FIREBASE_*` 一樣的做法（`.env` 本機測試，或部署平台/GitHub Actions 的環境變數）：

- `VITE_GOOGLE_API_KEY`：剛剛建立的金鑰
- `VITE_GOOGLE_API_URL`：要匯入的資料夾分享連結，例如 `https://drive.google.com/drive/folders/xxxxxxxx`

兩個變數都是選填，沒設定的話按鈕會分別跳出「尚未設定 Google API 金鑰」或「尚未設定雲端連結」的錯誤，不影響其他功能。GitHub Pages 部署要在 repo 設定的 **Variables**（不是 Secrets，跟 `VITE_FIREBASE_*` 同一類）裡加上這兩個名稱一致的變數。

> 這把金鑰只認得到「公開分享」的內容，讀不到私人未分享的檔案，所以外流也頂多是能看到你們本來就打算公開分享的題庫資料夾。

**4. 本機測試用範本資料（不打真的 API）**

反覆點「從雲端讀取」測畫面（分類篩選、已答過標籤、選項預覽）會真的對 Google 發送大量請求，重複測幾次就可能觸發它的異常流量偵測（連 CORS header 都不給的那種擋法，開發時就踩過一次）。本機開發不需要驗證真實資料時，在 `.env` 加：

```
VITE_MOCK_GOOGLE_DRIVE="true"
```

開著的話「從雲端讀取」會回傳寫死的本機範本題目（不會真的連線 Google），按鈕旁邊會顯示黃色的「測試模式」提示，不會誤以為是真資料。**只能放在本機 `.env`，絕對不要加進部署平台的環境變數**——正式站需要讀到真的資料夾內容。

---

## 疑難排解

| 狀況 | 原因 |
| --- | --- |
| 一直停在「連線中…」 | 匿名登入沒啟用（僅在閘門開啟時需要） |
| 「無法連線」 | 網路問題，或規則尚未發布 |
| 輸入碼後說「邀請碼不正確」 | `config/invite` 不存在、欄位名不是 `code`，或規則沒發布 |
| 開了閘門卻沒跳出畫面 | `requireInvite` 存成字串 `"true"` 而不是布林值 |

---

## 注意：房間代號變更史

聊天紀錄存在 `rooms/{房間代號}/messages`。

| 時期 | 房間代號 |
| --- | --- |
| 最早 | `DUAL-1105-1115` |
| 中期 | `MAIN-ROOM`（所有人共用一間） |
| 現在 | `pair__姓名A__姓名B`（每組一間） |

`MAIN-ROOM` 的資料還在，可以用後台的搬移按鈕拉進新的配對房間。
規則裡也為它留了一個例外（任何登入者都讀得到），等大家都搬完之後，
可以把 `firestore.rules` 裡的 `isLegacyRoom` 那段拿掉。

---

## 資料結構版本

目前為 **v4**（`DATA_SCHEMA_VERSION`，定義在 `src/types.ts`）。

| 版本 | 變更 |
| --- | --- |
| v2 | 對話紀錄移到 `messages` 子集合 |
| v3 | 訊息帶引用快照 `replyTo` |
| v4 | 多選作答、`rounds` 出題紀錄、分類重玩重置 |

**相容性**：v2 之後新增的欄位全部是選填。舊題目只存單一 `targetAnswer` / `initiatorGuess`，讀取時會自動視為長度 1 的陣列，所以舊的揭曉紀錄照常顯示，不需要做資料遷移。

新增的子集合 `rooms/{code}/rounds` 記錄每次出題，用途有二：判斷哪些題目已經玩過、統計最近 3 小時的題數。安全規則已一併更新（append-only），**請重新發布 `firestore.rules`**，否則出題會寫入失敗。

---

## 備份與還原

Firestore 的官方 managed export 需要 Blaze 方案，所以改用一般 SDK 走訪整個資料庫。兩個按鈕都在後台管理右上角的工具列。

### 下載備份

**資料庫圖示** → 一鍵下載完整備份 JSON。

內容包含**題庫、分類、房間狀態、對話紀錄、出題歷史**，連子集合都遞迴倒出來。

備份範圍是**你看得到的東西**——共用題庫加上你有參與的每一組對話。
別人的對話規則本來就不給讀，所以也備份不到（還原同理）。Timestamp 以 `{__type__:'timestamp', seconds, nanoseconds}` 的形式保存，不會在 JSON 轉換中失真。

> 旁邊的下載圖示是另一個功能「匯出題庫」，只含題目與分類，兩者用途不同。

### 從備份還原

**還原圖示**（箱子）→ 選擇備份檔 → 確認視窗會顯示備份時間、**備份對象**、文件數、結構版本 → 按「清空並還原」。

**選錯檔案會直接擋下來**：檔案裡的 `roomId` 不是目前這一組時，會跳出
「這份備份不屬於這個對話，檔案裡是【○○ 與 ××】的資料」並中止，不會刪掉任何東西。
判斷依據是檔案內容而不是檔名，改過檔名也騙不過去。

流程是**先清空這一組的子集合，再寫入備份內容**，範圍僅限這組對話。

⚠️ **不可復原**。建議還原前先按一次備份鈕保存目前狀態。

房間文件本身是**覆寫而不是刪除**，參與者名單也以現況為準——安全規則就是靠那份名單判斷誰能存取，刪掉它之後每一筆寫入都會被拒。

### 安全規則

為了讓還原能真正「先清空」，`messages` 與 `rounds` 已從唯讀追加改為允許刪除。**修改後請重新發布 `firestore.rules`**，否則還原會卡在清空階段。

---

## 測試安全規則

規則是這個 App 唯一的防線，所以附了一組測試：

```bash
npm install
npm run test:rules
```

會用 Firestore 模擬器實際跑一遍「對的密碼進得去、錯的進不去、
第三人讀不到別人的對話」等情境。需要 **Java 21 以上**。

> 這組測試抓過一個真實的漏洞：房間的讀取規則原本用 `resource == null`
> 容忍「文件還不存在」，但授權**查詢**時 `resource` 沒有綁定，那句變成恆真，
> 等於任何登入者都能列出全部房間文件。現在 `get` 與 `list` 分開寫，
> 查詢那條不給這個容忍。改動這一段之後務必重跑測試。

### 開著模擬器自己玩

想在不碰正式資料的情況下測功能，開兩個終端機：

```bash
npm run emulators   # 終端機 A：啟動 Firestore + Auth 模擬器
npm run dev:local   # 終端機 B：讓網站連到模擬器
```

`dev:local` 是 `vite --mode emulator`，會載入 `.env.emulator` 裡的
`VITE_USE_EMULATOR=true`，`src/lib/firebase.ts` 看到它才會呼叫
`connectFirestoreEmulator`。**其他任何指令（含正式 build）都連正式專案**，
不可能不小心把模擬器設定帶上線。

- 模擬器管理介面：<http://127.0.0.1:4000>（可以直接看／改資料、看規則被拒的紀錄）
- **專案 ID 兩邊必須一致**：模擬器以 project id 分隔資料，`npm run emulators` 用
  `demo-youaskianswer`，`.env.emulator` 也把 `VITE_FIREBASE_PROJECT_ID` 設成同一個。
  不一致的話 App 寫進一個命名空間、UI 顯示另一個，看起來就像完全沒資料
- 資料是**暫時的**，關掉就沒了。要保留下次接著用：
  `npx firebase-tools@14 emulators:start --project rules-test --only firestore,auth --import ./.emulator-data --export-on-exit`
- 模擬器裡是空的資料庫，所以帳號要重新註冊一次（自己設定密碼，至少 8 個字元）
- 邀請碼閘門在模擬器裡預設是關的（沒有 `config/access` 這份文件）

`firebase-tools` 體積很大（連帶幾百個套件），所以**沒有**放進 `devDependencies`——
測試指令用 `npx` 臨時抓，只有你要跑測試時才會下載，CI 的 `npm ci` 不受影響。
